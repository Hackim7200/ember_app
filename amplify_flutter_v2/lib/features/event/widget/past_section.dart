import 'package:flutter/material.dart';

class PastSection extends StatelessWidget {
  const PastSection({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(child: Text("PastSection"));
  }
}
